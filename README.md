# Poyesis Env CLI

Create/sync/pull `.env` files via the Poyesis Env API.

## Install & Run (via npx)

```bash
npx env create
npx env sync
npx env pull
npx env pull <secret> --path .production.env
```
